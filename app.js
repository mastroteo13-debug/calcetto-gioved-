const PAGES=[['dashboard','📊 Dashboard'],['partite','⚽ Partite'],['classifica','🏆 Classifica'],['pagella','🏅 Pagella'],['ricordi','📸 Ricordi'],['scheda','🪪 Scheda Giocatore'],['giocatori','👤 Giocatori'],['regolamento','📜 Regolamento'],['luogo','📍 Luogo'],['settings','⚙️ Settings']];
const esc=x=>String(x??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const sb=supabase.createClient(window.SUPABASE_URL,window.SUPABASE_KEY);let db={season:null,players:[],matches:[],settings:null,ratings:[],memories:[]};const nav=document.getElementById('nav'),app=document.getElementById('app');
function go(id){history.pushState({},'',`#${id}`);render(id)}window.onpopstate=()=>render(location.hash.slice(1)||'dashboard');
function navRender(a){nav.innerHTML=PAGES.map(([id,l])=>`<button class="${id===a?'active':''}" onclick="go('${id}')">${l}</button>`).join('')}
function layout(t,b){return `<div class="wrap"><div class="page-title"><div><div class="eyebrow">CALCETTO MANAGER</div><h1>${t}</h1></div><span class="season">${esc(db.season?.name||'2026/27')}</span></div>${b}</div>`}
async function load(){const {data:s,error:e}=await sb.from('seasons').select('*').eq('active',true).single();if(e)throw e;db.season=s;const [p,m,r,set,mem]=await Promise.all([sb.from('players').select('*').eq('season_id',s.id).order('name'),sb.from('matches').select('*').eq('season_id',s.id).order('match_date',{ascending:false}),sb.from('ratings').select('*'),sb.from('settings').select('*').eq('season_id',s.id).single(),sb.from('memories').select('*').eq('season_id',s.id).order('memory_date',{ascending:false})]);if(p.error||m.error||r.error||set.error||mem.error)throw(p.error||m.error||r.error||set.error||mem.error);db.players=p.data||[];db.matches=m.data||[];db.ratings=r.data||[];db.settings=set.data;db.memories=mem.data||[];document.body.classList.toggle('dark',!!db.settings?.dark_mode)}
async function render(id){navRender(id);try{if(!db.season)await load();const f={dashboard,partite,classifica,pagella,ricordi,scheda,giocatori,regolamento,luogo,settings}[id]||dashboard;await f()}catch(e){app.innerHTML=layout('ERRORE',`<div class="panel notice">${esc(e.message||e)}<br><br>Controlla la configurazione Supabase.</div>`)}}
async function matchPlayers(mid){const {data,error}=await sb.from('match_players').select('*,players(id,name,role)').eq('match_id',mid);if(error)throw error;return data||[]}
async function stats(){const out={};db.players.forEach(p=>out[p.id]={p,g:0,a:0,y:0,r:0,og:0,pen:0,sub:0,change:0,app:0});if(!db.matches.length)return Object.values(out);const {data,error}=await sb.from('match_players').select('*').in('match_id',db.matches.map(m=>m.id));if(error)throw error;(data||[]).forEach(x=>{const z=out[x.player_id];if(!z)return;z.app+=x.appearance?1:0;z.g+=x.goals||0;z.a+=x.assists||0;z.y+=x.yellow||0;z.r+=x.red||0;z.og+=x.own_goals||0;z.pen+=x.penalties||0;z.sub+=x.conceded||0;z.change+=x.substituted?1:0});return Object.values(out)}
function table(s){return `<div class="table-wrap"><table class="table"><thead><tr><th>#</th><th>Nome</th><th>Ruolo</th><th>Partite</th><th>Gol</th><th>Assist</th><th>Cart.</th><th>Aut.</th><th>Rig.</th><th>Subiti</th><th>Cambio</th></tr></thead><tbody>${s.map((x,i)=>`<tr onclick="goPlayer('${x.p.id}')"><td>${i+1}</td><td><b>${esc(x.p.name)}</b></td><td><span class="badge">${esc(x.p.role)}</span></td><td>${x.app}</td><td>${x.g}</td><td>${x.a}</td><td>${x.y+x.r}</td><td>${x.og}</td><td>${x.pen}</td><td>${x.sub}</td><td>${x.change}</td></tr>`).join('')}</tbody></table></div>`}
async function dashboard(){const s=await stats(),top=[...s].sort((a,b)=>b.g-a.g||b.a-a.a)[0],last=db.matches[0],goals=s.reduce((n,x)=>n+x.g,0);app.innerHTML=layout('PANORAMICA',`<div class="toolbar"><span>Stagione <b>${esc(db.season.name)}</b></span><button class="btn" onclick="render('dashboard')">↻ Aggiorna</button></div><div class="cards"><div class="stat"><b>${db.matches.length}</b><span>Partite</span></div><div class="stat"><b>${db.players.filter(p=>p.status==='Attivo'||p.active).length}</b><span>Giocatori attivi</span></div><div class="stat"><b>${goals}</b><span>Gol totali</span></div><div class="stat"><b>${s.reduce((n,x)=>n+x.y+x.r,0)}</b><span>Cartellini</span></div></div><div class="grid2"><div class="panel"><h2>⚽ Ultima partita</h2>${last?`<div class="match-preview"><div><strong>${esc(last.team_a_name)}</strong></div><div class="score">${last.score_a} <small>–</small> ${last.score_b}<em>${esc(last.match_date)} · ${esc(last.format)}</em></div><div><strong>${esc(last.team_b_name)}</strong></div></div><button class="btn primary" onclick="openMatch('${last.id}')">Dettaglio partita</button>`:'<div class="empty">Nessuna partita registrata.</div>'}</div><div class="panel"><h2>🏅 TOP SCORER</h2>${top?.g?`<div class="big-number">${top.g}</div><b>${esc(top.p.name)}</b>`:'<div class="empty">Nessun dato.</div>'}</div></div><div class="panel"><div class="toolbar"><h2>STATISTICHE STAGIONE</h2><button class="btn" onclick="go('classifica')">Classifica completa →</button></div>${table(s.sort((a,b)=>b.g-a.g||b.a-a.a))}</div>`)}
function matchCard(m){const status=m.played?'CONCLUSA':'DA DISPUTARE',winner=m.played&&m.score_a!==m.score_b?(m.score_a>m.score_b?'A':'B'):'';return `<div class="panel match-card"><div class="match-card-top"><div><span class="muted">${esc(m.match_date)}</span><span class="badge">${esc(m.format)}</span></div><span class="status ${m.played?'':'pending'}">${status}</span></div><div class="match-preview"><div class="team-name ${winner==='A'?'winner':''}"><span class="team-dot a"></span><strong>${esc(m.team_a_name)}</strong>${winner==='A'?'<small>VINCITRICE</small>':''}</div><div class="score">${m.score_a}<small>–</small>${m.score_b}<em>${m.played?'Risultato':'In programma'}</em></div><div class="team-name ${winner==='B'?'winner':''}"><span class="team-dot b"></span><strong>${esc(m.team_b_name)}</strong>${winner==='B'?'<small>VINCITRICE</small>':''}</div></div><div class="toolbar match-actions"><button class="btn primary" onclick="openMatch('${m.id}')">Dettaglio partita →</button><button class="btn danger" onclick="deleteMatch('${m.id}')">Elimina</button></div></div>`}
async function partite(){app.innerHTML=layout('PARTITE',`<div class="toolbar"><div><span class="eyebrow">STAGIONE ${esc(db.season.name)}</span><h2>${db.matches.length} partite</h2></div><button class="btn primary" onclick="newMatchForm()">＋ Nuova partita</button></div>${db.matches.map(matchCard).join('')||'<div class="panel empty">Nessuna partita. Crea la prima partita della stagione.</div>'}`)}
function newMatchForm(){app.innerHTML=layout('NUOVA PARTITA',`<form class="panel form" onsubmit="createMatch(event)"><div class="form-grid"><label>Data<input id="mdate" type="date" required value="${new Date().toISOString().slice(0,10)}"></label><label>Formato<select id="mformat"><option>6 VS 6</option><option>5 VS 5</option><option>7 VS 7</option><option>8 VS 8</option></select></label><label>Squadra A<input id="teamA" value="SQUADRA A"></label><label>Squadra B<input id="teamB" value="SQUADRA B"></label><label>Stato<select id="mplayed"><option value="true">Disputata</option><option value="false">Da disputare</option></select></label></div><div class="score-input"><label>Squadra A<input id="sa" type="number" min="0" value="0"></label><b>–</b><label>Squadra B<input id="sb" type="number" min="0" value="0"></label></div><h2>Giocatori</h2><p class="muted">Assegna ogni giocatore alla squadra. I giocatori non assegnati non compariranno nella partita.</p><div class="player-pick">${db.players.filter(p=>p.active!==false&&p.status!=='Inattivo').map(p=>`<div class="pick"><b>${esc(p.name)}</b><select data-pid="${p.id}"><option value="">—</option><option value="A">Squadra A</option><option value="B">Squadra B</option></select></div>`).join('')}</div><div class="toolbar"><button type="button" class="btn" onclick="go('partite')">Annulla</button><button class="btn primary">💾 Salva partita</button></div></form>`)}
async function createMatch(e){e.preventDefault();const {data:m,error}=await sb.from('matches').insert({season_id:db.season.id,match_date:mdate.value,format:mformat.value,played:mplayed.value==='true',score_a:+sa.value||0,score_b:+sb.value||0,team_a_name:teamA.value||'SQUADRA A',team_b_name:teamB.value||'SQUADRA B'}).select().single();if(error)return alert(error.message);const rows=[...document.querySelectorAll('[data-pid]')].filter(x=>x.value).map(x=>({match_id:m.id,player_id:x.dataset.pid,team:x.value,appearance:true,goals:0,assists:0,yellow:0,red:0,own_goals:0,penalties:0,conceded:0,substituted:false}));if(rows.length){const {error:e2}=await sb.from('match_players').insert(rows);if(e2){await sb.from('matches').delete().eq('id',m.id);return alert(e2.message)}}await load();go('partite')}
function winnerFor(m){if(!m.played||m.score_a===m.score_b)return '';return m.score_a>m.score_b?'A':'B'}
function prediction(m,rows){const a=rows.filter(x=>x.team==='A').length,b=rows.filter(x=>x.team==='B').length;if(!a&&!b)return 'Dati insufficienti';if(a===b)return 'Equilibrio perfetto';return a>b?`${m.team_a_name} favorita`:`${m.team_b_name} favorita`}
function voteSummary(id){const rs=db.ratings.filter(x=>x.match_id===id);if(!rs.length)return '<div class="empty">Nessun voto inserito per questa partita.</div>';const by={};rs.forEach(r=>{by[r.player_id]=r.vote});const vals=Object.entries(by).filter(([,v])=>v!==null&&v!=='').sort((a,b)=>b[1]-a[1]);const avg=rs.reduce((n,x)=>n+Number(x.vote||0),0)/rs.length;return `<div class="vote-summary"><div class="cards"><div class="stat"><b>${avg.toFixed(2)}</b><span>Media partita</span></div><div class="stat"><b>${rs.length}</b><span>Voti</span></div><div class="stat"><b>${vals[0]?esc(db.players.find(p=>p.id===vals[0][0])?.name||'—'):'—'}</b><span>Voto più alto</span></div></div></div>`}
async function openMatch(id){const m=db.matches.find(x=>x.id===id);if(!m)return;const rows=await matchPlayers(id),win=winnerFor(m);app.innerHTML=layout('DETTAGLIO PARTITA',`<div class="toolbar"><button class="btn" onclick="go('partite')">← Partite</button><div><button class="btn" onclick="shareMatch('${m.id}')">Condividi</button><button class="btn" onclick="goPagella('${m.id}')">🏅 Riepilogo Voti</button></div></div><div class="panel match-hero"><div class="match-meta"><span>${esc(m.match_date)}</span><span class="badge">${esc(m.format)}</span><span class="status ${m.played?'':'pending'}">${m.played?'CONCLUSA':'DA DISPUTARE'}</span></div><div class="hero-score"><div class="hero-team ${win==='A'?'win':''}"><span class="crest a">A</span><strong>${esc(m.team_a_name)}</strong>${win==='A'?'<small>🏆 VINCITRICE</small>':''}</div><div class="hero-center"><b>${m.score_a}</b><span>–</span><b>${m.score_b}</b></div><div class="hero-team ${win==='B'?'win':''}"><span class="crest b">B</span><strong>${esc(m.team_b_name)}</strong>${win==='B'?'<small>🏆 VINCITRICE</small>':''}</div></div></div><div class="panel prediction"><div><span class="eyebrow">PREVISIONE PARTITA</span><h2>${esc(prediction(m,rows))}</h2></div><span class="prediction-icon">⚡</span></div><div class="pitch-teams"><div class="team"><h3>🟢 ${esc(m.team_a_name)}</h3><div class="field">${rows.filter(x=>x.team==='A').map(x=>`<div class="player"><b>${esc(x.players?.name)}</b><span>⚽ ${x.goals||0} · 🎯 ${x.assists||0}</span></div>`).join('')||'<div class="empty">Nessun giocatore</div>'}</div></div><div class="team dark"><h3>🔵 ${esc(m.team_b_name)}</h3><div class="field">${rows.filter(x=>x.team==='B').map(x=>`<div class="player"><b>${esc(x.players?.name)}</b><span>⚽ ${x.goals||0} · 🎯 ${x.assists||0}</span></div>`).join('')||'<div class="empty">Nessun giocatore</div>'}</div></div></div><div class="panel"><div class="toolbar"><h2>Statistiche per giocatore</h2><button class="btn primary" onclick="editMatchStats('${m.id}')">✎ Modifica statistiche</button></div>${matchStatsTable(rows)}</div><div class="panel"><div class="toolbar"><h2>Riepilogo Voti</h2><button class="btn" onclick="goPagella('${m.id}')">Gestisci pagella →</button></div>${voteSummary(m.id)}</div><div class="toolbar"><button class="btn" onclick="go('partite')">Chiudi</button><button class="btn danger" onclick="deleteMatch('${m.id}')">Elimina partita</button></div>`)}
function shareMatch(id){const m=db.matches.find(x=>x.id===id);if(!m)return;const text=`Calcetto Manager — ${m.team_a_name} ${m.score_a}-${m.score_b} ${m.team_b_name} · ${m.match_date}`;if(navigator.share)navigator.share({title:'Calcetto Manager',text,url:location.href}).catch(()=>{});else navigator.clipboard?.writeText(text).then(()=>alert('Riepilogo copiato negli appunti.'))}
function matchStatsTable(r){return `<div class="table-wrap"><table class="table"><thead><tr><th>Giocatore</th><th>Squadra</th><th>Gol</th><th>Assist</th><th>Giallo</th><th>Rosso</th><th>Aut.</th><th>Rig.</th><th>Sub.</th><th>Cambio</th></tr></thead><tbody>${r.map(x=>`<tr><td><b>${esc(x.players?.name)}</b></td><td>${x.team}</td><td>${x.goals||0}</td><td>${x.assists||0}</td><td>${x.yellow||0}</td><td>${x.red||0}</td><td>${x.own_goals||0}</td><td>${x.penalties||0}</td><td>${x.conceded||0}</td><td>${x.substituted?'✓':'—'}</td></tr>`).join('')}</tbody></table></div>`}
async function editMatchStats(id){const r=await matchPlayers(id);app.innerHTML=layout('STATISTICHE PARTITA',`<form class="panel form" onsubmit="saveMatchStats(event,'${id}')">${r.map((x,i)=>`<div class="stat-edit"><b>${esc(x.players?.name)}</b><span class="badge">Squadra ${x.team}</span><label>Gol<input name="g${i}" type="number" min="0" value="${x.goals||0}"></label><label>Assist<input name="a${i}" type="number" min="0" value="${x.assists||0}"></label><label>Gialli<input name="y${i}" type="number" min="0" value="${x.yellow||0}"></label><label>Rossi<input name="r${i}" type="number" min="0" value="${x.red||0}"></label><label>Autogol<input name="og${i}" type="number" min="0" value="${x.own_goals||0}"></label><label>Rigori<input name="pen${i}" type="number" min="0" value="${x.penalties||0}"></label><label>Subiti<input name="sub${i}" type="number" min="0" value="${x.conceded||0}"></label><label class="check"><input name="ch${i}" type="checkbox" ${x.substituted?'checked':''}> Cambio</label></div>`).join('')}<div class="toolbar"><button type="button" class="btn" onclick="openMatch('${id}')">Annulla</button><button class="btn primary">Salva statistiche</button></div></form>`)}
async function saveMatchStats(e,id){e.preventDefault();const r=await matchPlayers(id);for(let i=0;i<r.length;i++){const q={goals:+e.target[`g${i}`].value||0,assists:+e.target[`a${i}`].value||0,yellow:+e.target[`y${i}`].value||0,red:+e.target[`r${i}`].value||0,own_goals:+e.target[`og${i}`].value||0,penalties:+e.target[`pen${i}`].value||0,conceded:+e.target[`sub${i}`].value||0,substituted:!!e.target[`ch${i}`].checked};const {error}=await sb.from('match_players').update(q).eq('id',r[i].id);if(error)return alert(error.message)}await load();openMatch(id)}
async function deleteMatch(id){if(!confirm('Eliminare definitivamente questa partita?'))return;const {error}=await sb.from('matches').delete().eq('id',id);if(error)return alert(error.message);await load();go('partite')}
async function classifica(){const s=await stats();s.sort((a,b)=>b.g-a.g||b.a-a.a||b.app-a.app);app.innerHTML=layout('CLASSIFICA',`<div class="panel"><div class="toolbar"><h2>🏆 Classifica ${esc(db.season.name)}</h2></div>${table(s)}</div>`)}
function goPagella(id){location.hash='pagella';render('pagella').then(()=>{if(id)document.getElementById('matchSelect').value=id,loadPagella(id)})}
async function pagella(){const id=db.matches[0]?.id||'';app.innerHTML=layout('PAGELLA',`<div class="panel form"><div class="toolbar"><div><span class="eyebrow">VALUTAZIONI</span><h2>Pagella partita</h2></div><select id="matchSelect" onchange="loadPagella(this.value)">${db.matches.map(m=>`<option value="${m.id}" ${m.id===id?'selected':''}>${esc(m.match_date)} · ${esc(m.team_a_name)} ${m.score_a}-${m.score_b} ${esc(m.team_b_name)}</option>`).join('')||'<option value="">Nessuna partita</option>'}</select></div><div id="pagellaBody">${id?'<div class="empty">Caricamento…</div>':'<div class="empty">Crea una partita prima di compilare la pagella.</div>'}</div></div>`);if(id)await loadPagella(id)}
async function loadPagella(id){if(!id)return;const rows=await matchPlayers(id),existing=db.ratings.filter(x=>x.match_id===id),map={};existing.forEach(x=>map[x.player_id]=x.vote);const max=Object.values(map).filter(v=>v!==null&&v!==''&&Number.isFinite(Number(v))).reduce((a,v)=>Math.max(a,Number(v)),-Infinity);document.getElementById('pagellaBody').innerHTML=`<form onsubmit="savePagella(event,'${id}')"><p class="muted">Il ✓ si assegna automaticamente al voto più alto. Nessun MVP.</p><div class="rating-grid">${rows.map((x,i)=>{const v=map[x.player_id]??'';return `<div class="rating-card ${v!==''&&Number(v)===max?'top-vote':''}" data-rating-card data-player="${x.player_id}"><div><b>${esc(x.players?.name)}</b><span class="badge">Squadra ${x.team}</span></div><label>Voto<input class="vote-input" name="v${i}" data-player="${x.player_id}" type="number" min="0" max="10" step="0.1" value="${v}" oninput="refreshBestVotes()"></label><label class="best-check"><input type="checkbox" disabled ${v!==''&&Number(v)===max?'checked':''}> ✓ voto più alto</label></div>`}).join('')}</div><div class="toolbar"><button class="btn primary">💾 Salva pagella</button></div></form>`}
function refreshBestVotes(){const inputs=[...document.querySelectorAll('.vote-input')],vals=inputs.map(x=>({el:x,v:Number(x.value)})).filter(x=>x.el.value!==''&&Number.isFinite(x.v));const max=vals.length?Math.max(...vals.map(x=>x.v)):-Infinity;document.querySelectorAll('[data-rating-card]').forEach(c=>{const input=c.querySelector('.vote-input'),check=c.querySelector('.best-check input'),best=input.value!==''&&Number(input.value)===max;c.classList.toggle('top-vote',best);check.checked=best})}
async function savePagella(e,id){e.preventDefault();const rows=await matchPlayers(id);for(let i=0;i<rows.length;i++){const vote=e.target[`v${i}`].value===''?null:Number(e.target[`v${i}`].value);const player=rows[i].player_id;const old=db.ratings.find(x=>x.match_id===id&&x.player_id===player);let res;if(old)res=await sb.from('ratings').update({vote}).eq('id',old.id);else if(vote!==null)res=await sb.from('ratings').insert({match_id:id,player_id:player,vote});if(res?.error)return alert(res.error.message)}await load();loadPagella(id)}
async function ricordi(){app.innerHTML=layout('RICORDI',`<div class="panel"><div class="toolbar"><h2>📸 Ricordi</h2><button class="btn primary">＋ Carica</button></div><div class="memory-grid">${db.memories.map(x=>`<div class="memory"><div class="memory-placeholder">📷</div><h3>${esc(x.title||'Ricordo')}</h3><span>${esc(x.memory_date||'')}</span><p>${esc(x.description||'')}</p></div>`).join('')||'<div class="empty">Nessun ricordo.</div>'}</div></div>`)}
async function giocatori(){app.innerHTML=layout('GIOCATORI',`<div class="panel"><div class="toolbar"><h2>👤 Giocatori</h2><span>${db.players.length} giocatori</span></div>${table((await stats()).sort((a,b)=>a.p.name.localeCompare(b.p.name)))}</div>`)}
function goPlayer(id){location.hash='scheda';render('scheda');setTimeout(()=>playerPage(id),0)}
async function playerPage(id){const p=db.players.find(x=>x.id===id);if(!p)return;const s=(await stats()).find(x=>x.p.id===id);app.innerHTML=layout('SCHEDA GIOCATORE',`<div class="panel profile"><div class="avatar">${esc(p.name[0]||'?')}</div><h2>${esc(p.name)}</h2><span class="badge">${esc(p.role||'—')}</span><span class="status ${p.injured?'injured':''}">${p.injured?'Infortunato':esc(p.status||'Attivo')}</span><div class="cards"><div class="stat"><b>${s.app}</b><span>Presenze</span></div><div class="stat"><b>${s.g}</b><span>Gol</span></div><div class="stat"><b>${s.a}</b><span>Assist</span></div><div class="stat"><b>${s.y+s.r}</b><span>Cartellini</span></div></div></div>`)}
async function scheda(){const p=db.players[0];if(p)return playerPage(p.id)}
async function regolamento(){app.innerHTML=layout('REGOLAMENTO',`<div class="panel rules"><h2>📜 Regolamento</h2><p>Regolamento della stagione ${esc(db.season.name)}.</p></div>`)}
async function luogo(){app.innerHTML=layout('LUOGO',`<div class="panel place"><h2>📍 Luogo</h2><p>Via Carreggia, 21010 Cardano al Campo VA</p><button class="btn primary" onclick="window.open('https://www.google.com/maps/search/?api=1&query=Via+Carreggia+21010+Cardano+al+Campo+VA','_blank')">Apri su Maps</button></div>`)}
async function settings(){app.innerHTML=layout('SETTINGS',`<div class="panel"><h2>⚙️ Impostazioni</h2><div class="setting"><div><b>Modalità scura</b><small>Salvata nel database.</small></div><input type="checkbox" ${db.settings?.dark_mode?'checked':''} onchange="toggleDark(this.checked)"></div><div class="setting"><div><b>Stagione</b><small>${esc(db.season.name)}</small></div></div></div>`)}
async function toggleDark(v){const {error}=await sb.from('settings').update({dark_mode:v}).eq('season_id',db.season.id);if(error)return alert(error.message);db.settings.dark_mode=v;document.body.classList.toggle('dark',v)}
render(location.hash.slice(1)||'dashboard');